# Nightly Memory Distill Prompt

Review the latest runtime memory index and checkpoints.

Produce:

1. candidate durable memories worth keeping in `M365`
2. recent warnings that should stay in `M30`
3. proven patterns that deserve `M90`
4. stale or duplicate items that should not be promoted

Do not rewrite product truth.
Do not collapse deterministic battle facts into vague summaries.
