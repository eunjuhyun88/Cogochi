# Cogochi Tech Debt Tracker

Last updated: 2026-03-07

## Open Items

1. Add real embedding-backed retrieval
2. Emit runtime benchmark manifests for every promotion-eligible eval
3. Replace local artifact manifests with provider-backed artifact registry persistence
4. Add async PvP snapshot persistence
5. Replace heuristic `trainingOrchestrator.ts` execution with worker-backed jobs
6. Replace local `fineTuneService.ts` manifests with real SFT/LoRA backends
7. Add rollback-safe active artifact switching in the UI
